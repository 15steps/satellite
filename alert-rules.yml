groups:
- name: cadAdvisorAlert
  rules:
  - alert: HightUsageCPU
    expr: rate(container_cpu_usage_seconds_total{name=~"devops_http.*"}[1m])*100 > 0.01
    for: 1m
    labels:
      severity: page
    annotations:
      summary: High CPU Utilization
